import {
  Box
} from "@mui/material"

export const SponsorTracks = () => {
  
  const ParentBox = styled(Box)({
    width: "100vw",
    height: "100vh",
    backgroundColor: "black"
  })

  return(
    <ParentBox>
    </ParentBox>
  )
}
