import React from "react";
import {motion} from "framer-motion";
// Custom Styles
import styles from "./track.module.scss";

// MUI components
import {
  Box,
  Typography
}
from "@mui/material";

export const Tracks = () => {
  const font = "'Lexend Exa', sans-serif";
  const Heading = styles(Typography)
  return(
    <></>
  )
}
